<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {# Inclusion de Bootstrap CSS #}
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        
        
        {% block stylesheets %}
        
        <style>

            nav.navbar{
                background-color: #012030;
                color: #fff;
            }

            .form-control {
                margin-right: 10px;
            }

            body{
                background-color: #13678A;
                color: #fff;
            }

        </style>

        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light sticky-top" style="background-color: #012030">
            <div class="container-fluid">
                <a class="navbar-brand" style="color:#fff" href="{{path('app_home')}}"><img style="width: 50px; height: 50px" src="{{ asset('media/logo.png')}}"> </i>EasyVoyage</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon" style="color:#fff important!"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <a class="nav-link active" style="color:#fff" aria-current="page" href="{{path('app_home')}}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{path('app_destination')}}">Destinations</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{path('app_flight')}}">Vols</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{path('app_hotel')}}">Hôtels</a>
                        </li>
                        {# <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">Link</a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" href="#">Vols</a>
                                <a class="dropdown-item" href="#">Hôtels</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="#">Something else here</a>
                            </div>
                        </li> #}
                        {# <li class="nav-item">
                            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
                        </li> #}
                    </ul>
                    <ul class="navbar-nav mr-4">
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{path('app_review')}}">Avis</a>
                        </li>
                    </ul>
                    <form class="form-inline my-2 my-lg-0">
                        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-outline-success my-2 my-sm-2" type="submit"><i class="bi bi-search-heart"></i></button>
                    </form>
                    
                    {% if app.user %}
                    <ul class="navbar-nav mr-4">
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{ path('app_myspace') }}">Mon Espace</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    </ul>
                    {% else %}
                    <ul class="navbar-nav mr-4">
                        <li class="nav-item">
                            <a class="nav-link" style="color:#fff" href="{{ path('app_login') }}">Login</a>
                        </li>
                    </ul>
                    {% endif %}
                </div>
            </div>
        </nav>

        {% block body %}{% endblock %}
        
        {# Inclusion de Bootstrap JS #}
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </body>
</html>
